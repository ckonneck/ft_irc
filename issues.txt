vllt temporary nickname
no such channel bei welcome u8: No such channel
serverpasswort implementen


Mode +o doesn't work yetttt even though serverchan says it works
:No such nick/channel;//I GET THIS DISPLAYED WHEN TRYING TO MODE +o SOMEONE.
+topic, wenn topicmode off is, kann jeder topic setzen, muss noch implementiert werden.

add PART support(parsing)(jan)


semidone:

invite, but needs further testing
topic, but needs optimization



so error messages vllt direkt solomessage in den chatroom nur an den der die errormessage kriegen
// nicht unbedingt moeglich/noetig, andere ircserver machen das genauso, bestimmte sachen nur
// im "clientwindow" zu displayen und nich im chatwindow.


wenn kein operator im raum ist, sollte der naechste in line operator kriegen 
//unsicher ob das so im protokoll is oder nich.


DONE;

FIXED!  nickchange sends to all chatrooms right now, not only the ones the dude is in
